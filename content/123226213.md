

+++
title = "使用脚本上传coredump文件至远端服务器"
template = "page.html"
date = 2022-03-02T11:31:35
[taxonomies]
tags = ["default"]
[extra]
+++

```
> cat /proc/sys/kernel/core_pattern
|/opt/crash_reporter.sh %E %t

> cat crash_reporter.sh
#!/bin/bash

# crash_reporter.sh %E %t
CRASH_FILE_NAME=${1##*!}
CRASH_TIME=`echo $2|date "+%Y%m%d%H%M%S"`

#FILE_NAME=/opt/a.txt
#echo $CRASH_FILE_NAME > ${FILE_NAME}
#echo $CRASH_TIME >> ${FILE_NAME}

# can use nginx webDav as file manage server
curl  -u username:password -X PUT --data-binary @- --no-buffer "http://yourwebsite/${CRASH_FILE_NAME}_${CRASH_TIME}"

```
---
**备份地址**: 【[使用脚本上传coredump文件至远端服务器](https://coderr.blog.csdn.net/article/details/123226213)】