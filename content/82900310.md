

+++
title = "nginx-supervisor-tornado配置"
template = "page.html"
date = 2018-09-29T17:15:19
[taxonomies]
tags = ["csdn_2"]
[extra]
+++

```
[group:tornadoes]
programs=tornado-8000,tornado-8001,tornado-8002,tornado-8003

[program:tornado-8000]
command=/root/.virtualenvs/ali_weapp/bin/python3 /tmp/pycharm_project_deploy/server.py --port=8000
directory=/tmp/pycharm_project_deploy
user=root
autorestart=true
autostart=true
redirect_stderr=true
stdout_logfile=/root/tornado_runlog/tornado0.log
loglevel=info

[program:tornado-8001]
command=/root/.virtualenvs/ali_weapp/bin/python3 /tmp/pycharm_project_deploy/server.py --port=8001
directory=/tmp/pycharm_project_deploy
user=root
autorestart=true
autostart=true
redirect_stderr=true
stdout_logfile=/root/tornado_runlog/tornado1.log
loglevel=info

[program:tornado-8002]
command=/root/.virtualenvs/ali_weapp/bin/python3 /tmp/pycharm_project_deploy/server.py --port=8002
directory=/tmp/pycharm_project_deploy
user=root
autorestart=true
autostart=true
redirect_stderr=true
stdout_logfile=/root/tornado_runlog/tornado2.log
loglevel=info

[program:tornado-8003]
command=/root/.virtualenvs/ali_weapp/bin/python3 /tmp/pycharm_project_deploy/server.py --port=8003
directory=/tmp/pycharm_project_deploy
user=root
autostart=true
autorestart=true
redirect_stderr=true
stdout_logfile=/root/tornado_runlog/tornado3.log
loglevel=info

```

---

```
# WebSocket 配置
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

upstream tornadoes {
    server 127.0.0.1:8000;
    server 127.0.0.1:8001;
    server 127.0.0.1:8002;
    server 127.0.0.1:8003;
}

server {
        listen 443;
        server_name wx.coderr.cn; # 改为绑定证书的域名

	#access_log  /var/log/nginx/https_wx_access.log  main;

        # ssl 配置
        ssl on;
        ssl_certificate wx_ssl/1_wx.coderr.cn_bundle.crt;
        ssl_certificate_key wx_ssl/2_wx.coderr.cn.key;

        ssl_session_timeout 5m;
        ssl_protocols TLSv1 TLSv1.1 TLSv1.2;
        ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:HIGH:!aNULL:!MD5:!RC4:!DHE;
        ssl_prefer_server_ciphers on;

        # WebSocket 配置
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

	# 静态文件的配置
        #location /static/ {
        #    root /tmp/pycharm_project_deploy/;
        #    if ($query_string) {
        #        expires max;
        #    }
        #}

        location / {

	  proxy_pass_header Server;
	  proxy_redirect off;
	  proxy_set_header X-Scheme $scheme;

          proxy_set_header X_FORWARDED_PROTO https;
	  proxy_set_header Host $host;
          proxy_set_header X-Real-IP $remote_addr;
          proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
	  real_ip_header X-Real-IP;
          #proxy_pass http://127.0.0.1:8000;
	  proxy_pass http://tornadoes;
        }
    }

```
---
**备份地址**: 【[nginx-supervisor-tornado配置](https://blog.csdn.net/sinat_29173167/article/details/82900310)】
