

+++
title = "termux 安装ubuntu"
template = "page.html"
date = 2022-09-16T16:23:10
[taxonomies]
tags = ['Virtualization/Containers & OS', 'backup_csdn']
[extra]
+++

* 下载ubuntu镜像
  https://github.com/MFDGaming/ubuntu-in-termux/
* 使用proot来chroot

```
#!/data/data/com.termux/files/usr/bin/bash
cd $(dirname $0)
## unset LD_PRELOAD in case termux-exec is installed
unset LD_PRELOAD
command="proot"
## uncomment following line if you are having FATAL: kernel too old message.
#command+=" -k 4.14.81"
command+=" --link2symlink"
command+=" -0"
command+=" -r ubuntu-fs"
#command+=" --kill-on-exit"
if [ -n "$(ls -A ubuntu-binds)" ]; then
    for f in ubuntu-binds/* ;do
      . $f
    done
fi
command+=" -b /dev"
command+=" -b /proc"
command+=" -b /sys"
command+=" -b /data/local/tmp/"
command+=" -b ubuntu-fs/tmp:/dev/shm"
command+=" -b /data/data/com.termux"
command+=" -b /:/host-rootfs"
command+=" -b /sdcard"
command+=" -b /storage"
command+=" -b /mnt"
command+=" -b /data/data/com.termux/files/home/.git/OpenSDK:/OpenSDK"
command+=" -w /root"
command+=" /usr/bin/env -i"
command+=" HOME=/root"
command+=" PATH=/usr/local/sbin:/usr/local/bin:/bin:/usr/bin:/sbin:/usr/sbin:/usr/games:/usr/local/games"
command+=" TERM=$TERM"
command+=" LANG=C.UTF-8"
command+=" /bin/bash --login"
com="$@"
if [ -z "$1" ];then
    exec $command
else
    $command -c "$com"
fi

```
---
**备份地址**: 【[termux 安装ubuntu](https://coderr.blog.csdn.net/article/details/126893016)】
