

+++
title = "进程扇和进程链的实现"
template = "page.html"
date = 2017-11-11T14:52:40
[taxonomies]
tags = ['backup_csdn', 'Linux System Programming & IPC']
[extra]
+++

```
#include "io.h"

//进程链
int main(int argc, char const *argv[])
{
    pid_t pid;
    int i = 0;
    int counter = atoi(argv[1]);
    for (i = 0; i < counter; ++i)
    {
        pid = fork();
        if (pid < 0) {

        }
        else if (pid == 0) {
            continue;
        }
        else {
            break;
        }
    }
    printf("pid:%d,ppid:%d\n", getpid(), getppid());
    sleep(3);
    return 0;
}

//进程扇
int main(int argc, char const *argv[])
{
    pid_t pid;
    int i = 0;
    int counter = atoi(argv[1]);
    for (i = 0; i < counter; ++i)
    {
        pid = fork();
        if (pid < 0) {

        }
        else if (pid == 0) {
            break;
        }
        else {
            continue;
        }
    }
    printf("pid:%d,ppid:%d\n", getpid(), getppid());
    sleep(3);
    return 0;
}

```
---
**备份地址**: 【[进程扇和进程链的实现](https://blog.csdn.net/sinat_29173167/article/details/78506525)】
