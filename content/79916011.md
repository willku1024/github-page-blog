

+++
title = "解决systemd 下redis不能启动"
template = "page.html"
date = 2018-04-12T16:32:30
[taxonomies]
tags = ['Linux System Programming & IPC', 'z_csdn', 'DevOps & Tools (Git/CMake/VSCode)']
[extra]
+++

```
[root@aliyun_1 ~]# vim /usr/lib/systemd/system/redis.service

[Unit]
Description=Redis persistent key-value database
After=network.target

[Service]
ExecStart=/usr/bin/redis-server /etc/redis.conf --daemonize yes
ExecStop=/usr/libexec/redis-shutdown
Type=forking
User=redis
Group=redis
#PIDFile=/var/run/redis.pid
RuntimeDirectory=redis
RuntimeDirectoryMode=0755

[Install]
WantedBy=multi-user.target

```

---

```
[Unit]
Description=Redis persistent key-value database
After=network.target

[Service]
ExecStart=/usr/bin/redis-server /etc/redis.conf --daemonize no
ExecStop=/usr/libexec/redis-shutdown
User=redis
Group=redis
RuntimeDirectory=redis
RuntimeDirectoryMode=0755

[Install]
WantedBy=multi-user.target

```
---
**备份地址**: 【[解决systemd 下redis不能启动](https://blog.csdn.net/sinat_29173167/article/details/79916011)】
